<% @coffee_shops.each do |shop| %>
  <%= link_to coffee_shop_path(shop) do %>
    <h3><%= shop.name %></h3>
  <% end %>
<% end %>

<%= link_to "Add a new Coffee Shop", new_coffee_shop_path, class: "btn btn-sm btn-primary" %>
